# Feature 50853

**Provisional label:** `100pct_failure_1`
**Confidence:** low
**Modal candidate concept (from top corpus category):** shopping_with_promo

## Logit lens — top promoted tokens

These tokens the feature most pushes the model toward (when fired):

` /` (+0.10), `abar` (+0.08), `undos` (+0.07), `ěl` (+0.07), `ukan` (+0.07), `krit` (+0.07), `akit` (+0.06), `ape` (+0.06), `ityEngine` (+0.06), `aset` (+0.06), `orang` (+0.06), ` Uncategorized` (+0.06), `rost` (+0.06), `MemoryWarning` (+0.06), `AGER` (+0.06)

## Logit lens — top suppressed tokens

These tokens the feature pushes the model AWAY from:

`iferay` (-0.09), `ばかり` (-0.09), `にして` (-0.08), ` 먼` (-0.08), `_________________

` (-0.08), ` Göz` (-0.08), `_Metadata` (-0.08), `мы` (-0.08), `.shapes` (-0.08), `алу` (-0.07)

## Decoder-direction neighbors

Features with the most similar decoder vectors (likely encoding related concepts):

- f61035 (cosine = 0.673)
- f39820 (cosine = 0.617)
- f61203 (cosine = 0.562)
- f51891 (cosine = 0.561)
- f24750 (cosine = 0.531)
- f48190 (cosine = 0.483)
- f57438 (cosine = 0.481)
- f32886 (cosine = 0.477)
- f14047 (cosine = 0.477)
- f36983 (cosine = 0.472)

## Top-activating corpus prompts

Prompts from a curated corpus where this feature fires strongest:

| Rank | Category | Activation | Prompt |
|---|---|---|---|
| 1 | shopping_with_promo | 0.08 | Today's Deal: Wireless Earbuds for $39.99 — Buy Now! |

## Notes

Hypothesis: this feature appears to encode something related to **shopping_with_promo**, based on the corpus categories where it activates strongest. The logit lens tokens above provide additional clues. The confidence rating reflects how consistent the evidence is across methods.

**Caveat:** This is provisional. The activations come from a small (~40-prompt) curated corpus. A larger, naturalistic probe would tighten the claim.